The file is flagged as sensitive and each write attempt is triggering a permission prompt for user approval. The research is complete; here is a summary of what I found and the full replacement content ready to paste once you approve the write.

---

**Key findings from live data (June 2026):**

1. **TSS99's addon-cutting #817 merged** — garrison approved in a single round with "LGTM." The prior closed PRs (#803, #804) had `[codex]` in the title. Branding was the killer, not the repo. Addon-cutting is now conditionally viable.

2. **LLM disclosure is a hard gate** — alexanderivrii explicitly blocked #16323 until the contributor added the AI/LLM disclosure checkboxes from the PR template. It's not optional.

3. **Multiple open PRs = spam flag** — alexanderivrii called out 9 concurrent PRs by @peter941221 as LLM spam (#16409 comment text: "Talking to LLMs is not a good use of reviewers' time"). The contributor survived by closing all but one. Keep ≤1 active PR.

4. **Rust is now in scope** — #16409 fixed `crates/transpiler/src/commutation_checker.rs` and merged. The transpiler is heavily Rust now; Python-only framing is outdated.

5. **New community fix evidence** — #16394 (MultiplierGate, Cryoris approved same day), #16323 (dagdependency global_phase, 1-line fix), #16175 (pi_check fractions, 3f) all follow the exact 3-file pattern.

Please approve the write permission prompt so I can save the rewritten `merged-patterns.md`. If the prompt has already timed out, I can retry any of the three tools (Write, Edit, or Bash cat) on your signal.

