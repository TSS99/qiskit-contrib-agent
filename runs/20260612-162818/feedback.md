I have enough signal across the PRs. Now writing the lessons.

- [FEEDBACK]: Avoid submitting multiple PRs concurrently — jakelishman batch-closed 8 PRs on 2026-05-04 citing "too much volume" of LLM PRs, explicitly stating "it is not worth the maintainer time to make an accurate determination"; slow down to one or two PRs at a time and drive each to completion before opening new ones.
- [FEEDBACK]: Keep PR descriptions brief — alexanderivrii (PR 16116) objected that "Validation", "Problem", and "Tests" subsections "feel unnecessary given that CI already covers this"; use the structure: one-sentence summary, short details, LLM attribution — nothing more.
- [FEEDBACK]: Validate the reproducer actually exhibits the bug before submitting — ShellyGarion asked for "a reproducing code example that actually shows the incorrect behaviour" on PR 16124, and alexanderivrii found the reproducer invalid mid-review; never open a bug-fix PR without first confirming the failure on unpatched main.
- [FEEDBACK]: Fix the correct layer, not the symptom — jakelishman CHANGES_REQUESTED on PR 16062 (QASM3 exporter unitary naming): "the root fault is not in the exporter but in the importer … fixing the output to work around an importer problem is wrong"; trace the bug to its origin before writing code.
- [FEEDBACK]: Ensure the fix does not regress valid adjacent behavior — jakelishman rejected PR 16116 (Sabre disjoint layout) as "overly restrictive" because the fix broke the valid case where idle qubits can be placed on a separate chip, and told TSS99 on PR 16124 that the stated fix "actually makes the example worse"; add tests for valid edge-cases that should still pass alongside the regression test.
- [FEEDBACK]: Adopt the technically superior approach when maintainers suggest it — PR 16309 (BitArray bitcount) merged after TSS99 accepted the subtract-overcount strategy (avoids array mutation, performs better on large arrays) proposed by aeddins-ibm and ihincks, rebased promptly, and responded to each technical comment; do not defend the initial implementation when reviewers propose a measurably better alternative.

